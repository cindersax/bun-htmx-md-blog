{
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "build": "bun build src/server.ts --outdir=./out --target=bun --minify --watch"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^11.1.0",
    "sharp": "^0.33.1"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "bun-types": "^1.0.7",
    "tailwindcss": "^3.4.0"
  }
}
